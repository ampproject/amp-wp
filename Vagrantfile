# Builds a local development environment with Docker inside Vagrant.

Vagrant.configure(2) do |config|
  config.vm.hostname = "ampwp"
end

load File.join(
  File.dirname(__FILE__),
  'vendor/wpsh/local/Vagrantfile'
)
