/**
 * Enum: Animation status.
 *
 * @property {number} stopped
 * @property {number} prepared
 * @property {number} playing
 * @property {number} finished
 *
 * @enum {number}
 */
export const ANIMATION_STATUS = Object.freeze( { stopped: 1, prepared: 2, playing: 3, finished: 4 } );
